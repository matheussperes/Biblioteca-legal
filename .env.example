# PostgreSQL (com extensão pgvector). Ver docker-compose.yml para subir localmente.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/knowledge_pipeline?schema=public"

# Chave da OpenAI — necessária para:
#   Fase 1: Steps 7 (Enriquecimento IA) e 8 (Embeddings).
#   Fase 2: classificação, extração de entidades, busca vetorial, reranking e geração de respostas.
OPENAI_API_KEY=""
